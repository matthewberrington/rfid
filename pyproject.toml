[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rfig"
description = "An Example Project"
version = "0.1"
readme = "README.md"
dependencies = ["paramiko", "sllurp", "keyboard", "pandas", "nicegui", "pyperclip"]
